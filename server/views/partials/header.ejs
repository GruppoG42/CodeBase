<!DOCTYPE html>
<html lang="en" class="bg-gray-200">
<head>
    <meta charset="UTF-8">
    <title><%= locals.title || "Tripply" %></title>
    <link rel="stylesheet" href="https://unpkg.com/tailwindcss@^1.0/dist/tailwind.min.css">
</head>
<body>
<nav class="flex items-center justify-between flex-wrap bg-gray-800 p-6">

    <div class="flex items-center flex-shrink-0 text-white mr-6">
       <span class="font-semibold text-xl tracking-tight">TRIPPLY</span>
    </div>

    <div class="w-full block flex-grow lg:flex lg:items-center lg:w-auto">
        <div class="text-sm lg:flex-grow">
            <% if (locals.user) { %>
                <a  href="/home"
                class="block mt-4 lg:inline-block lg:mt-0 text-gray-300 hover:text-white mr-4">Home/Itinerari community</a>
                <a  href="/myitineraries"
                    class="block mt-4 lg:inline-block lg:mt-0 text-gray-300 hover:text-white mr-4">I miei itinerari</a>
                <a  href="/saved"
                    class="block mt-4 lg:inline-block lg:mt-0 text-gray-300 hover:text-white mr-4">Salvati</a>
                <a  href="/createItinerary"
                    class="block mt-4 lg:inline-block lg:mt-0 text-gray-300 hover:text-white mr-4">Crea Itinerario</a>
                <a  href="/searchItineraries"
                    class="block mt-4 lg:inline-block lg:mt-0 text-gray-300 hover:text-white mr-4">Cerca Itinerari</a>
            <% } else { %>
                <a href="/" class="block mt-4 lg:inline-block lg:mt-0 text-gray-300 hover:text-white mr-4">Home</a>
                <a  href="/login"
                    class="block mt-4 lg:inline-block lg:mt-0 text-gray-300 hover:text-white mr-4">Login</a>
            <% } %>
        </div>
        <% if (locals.user) { %>
            <div class="flex items-center">
                <a href="/profile" class="flex items-center text-gray-300 hover:text-white">
                    <img class="h-8 w-8 rounded-full mr-2" src="<%= user.picture %>" alt="Profile picture">
                    <span>Profilo</span>
                </a>
            </div>
        <% } %>
    </div>
</nav>
<div class="flex justify-center py-4">
    <div class="w-5/6 lg:w-3/4 bg-white p-4 rounded overflow-hidden shadow-lg">
